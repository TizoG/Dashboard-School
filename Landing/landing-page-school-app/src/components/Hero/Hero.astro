---
import HeroImg from './components/Hero-img.astro';
---

<section
    class="relative min-h-screen flex items-center bg-gradient-to-br from-amber-50 to-blue-50 justify-center px-4"
>
    <div class="max-w-7xl mx-auto grid lg:grid-cols-2 gap-12 items-center">
        <div class="text-center lg:text-left space-y-8">
            <h1
                class="text-4xl md:text-5xl lg:text-6xl text-gray-900 leanding-tight font-display"
            >
                Organiza tu vida escolar con un solo clic
            </h1>
            <p class="text-lg md:text-xl text-gray-600 max-w-2xl">
                Dashboard Escolar es tu espacio digital para gestionar notas,
                tareas, archivos y más dede un solo lugar
            </p>
            <div
                class="flex flex-col sm:flex-row gap-4 justify-center lg:justify-start"
            >
                <button
                    class="bg-blue-600 hover:bg-blue-700 text-white px-8 py-3 rounded-full text-lg"
                    >Ver Demo</button
                >
                <button
                    class="bg-white border-4 border-blue-600 text-blue-600 hover:bg-gray-100 hover:text-black px-8 py-3 rounded-full"
                    >Empezar gratis</button
                >
            </div>
        </div>
        <div class="flex justify-center">
            <div class="relative">
                <div class="bg-white rounded-2xl shadow-2xl p-8 max-w-lg">
                    <HeroImg />
                </div>
                <div
                    class="absolute -top-4 -right-4 bg-amber-400 text-amber-900 px-4 py-2 rounded-full text-sm shadow-lg"
                >
                    Nuevo
                </div>
            </div>
        </div>
    </div>
</section>
